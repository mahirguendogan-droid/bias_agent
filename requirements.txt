
# requirements.txt — installed automatically by HuggingFace Spaces
openai>=1.0.0
# Pinned to the major version the UI is tested against: Gradio 6 moved `css`
# off the Blocks constructor, so an unbounded range can silently change layout.
gradio>=6.20,<7
pandas>=2.0.0
matplotlib>=3.8.0
numpy>=1.24.0
scipy>=1.11.0